claude \
  --dangerously-skip-permissions \
  --output-format=stream-json \
  --verbose \
  -p "请从零实现一个「单色图标转 SVG 工具」纯前端 Web 应用（React 18 + TypeScript + Vite，pnpm，Tailwind CSS），要求在视觉布局、交互逻辑、参数行为与导出结果上精准复刻如下功能与运行效果。不要引入占位实现；所有功能必须可运行。

一、核心目标
1) 用户导入一张图片（上传/拖拽/粘贴），左侧做预处理，右侧自动生成 SVG（矢量追踪）。
2) 左右两个预览区必须支持缩放 + 拖动，并且缩放倍率与平移视野严格同步：用户在任意一侧拖动/缩放，另一侧看到的位置与倍率一致。
3) 生成后的 SVG 支持复制/下载；支持“节点高亮”用于检查路径节点分布（节点点大小在缩放时保持屏幕像素恒定，不随预览缩放变大/变小）。

二、技术栈与依赖（必须使用这些，不要替换为其它库）
- react, react-dom, react-router-dom（使用 HashRouter，确保部署在任意子路径下也能打开，例如 /img2icon-svg/）
- zustand（全局状态）
- tailwindcss + clsx + tailwind-merge（样式/合并）
- imagetracerjs（SVG 矢量追踪核心）
- lucide-react（图标）
- Vite 构建，TypeScript 工程化，pnpm scripts: dev/build/preview/check（tsc -b --noEmit）
- PWA：vite-plugin-pwa，manifest 必须为：
  - name: 单色图标转 SVG 工具
  - short_name: 图标转SVG
  - description: 位图图标 PNG/JPG → 可编辑 SVG 矢量路径
  - start_url/scope: ./，display: standalone，background/theme color 与深色主题一致；图标放 public/（favicon.svg、apple-touch-icon.png、pwa-192.png、pwa-512.png）
- 不要引入或注入任何 badge 组件与插件（例如 vite-plugin-trae-solo-badge），页面右下角不应出现任何 badge。

三、页面与布局（必须按此结构实现）
1) 顶部标题区
- 主标题：单色图标转 SVG 工具
- 主标题右侧有一个小胶囊强调条（Sparkles 图标 + 文案）：位图图标 PNG/JPG → 可编辑 SVG 矢量路径
- 右上角“使用说明”图标按钮（CircleHelp）：鼠标悬停或点击均可显示说明弹层；点击后弹层可固定显示；点击弹层外区域关闭。

2) 主体双栏
- 左列：预处理区域（上：导入工具条 + 预览；下：预处理参数面板）
- 右列：矢量化区域（上：导出/信息/节点高亮工具条 + 预览；下：矢量化参数面板）
- 左右预览区高度必须一致。

四、导入图片与空态提示
1) 导入方式
- 点击“选择文件”按钮打开文件选择（accept=image/*）
- 支持拖拽文件到左侧预览区域导入
- 支持粘贴剪贴板图片导入（window paste 监听，识别 image/* item）
- “载入示例图”按钮生成内置示例（canvas 绘制后转 ImageData）
2) 空态引导
- 未导入图片时：“选择文件”按钮高亮；左侧预览区中央提示：拖入图片或者黏贴图片到此处
3) 拖拽反馈
- 拖拽悬停在预览区时显示明显 ring 高亮

五、预处理（左侧参数）
1) 预处理参数（Zustand）
- mode: none | binary | grayscale
- invert: boolean
- threshold: number (0-255)
- contrast: number (-60 to 60)
- backgroundRemoval: number (0-1)
- edgeEnhance: number (0-1)
- denoise: number (0-1)
- addBackground: string | null
2) UI（PreprocessPanel）
- 顶部：反相按钮 + Segmented（不处理/二值/灰度）
- Slider：阈值、对比度、背景去除、边缘增强、去噪
- 在“去噪”之后增加“透明通道背景色”标题，并在标题后添加说明图标（CircleHelp）。鼠标悬停显示 tooltip，tooltip 默认不占位（hidden），以“向右上角弹出”为主，避免被底部遮挡或截断。
  - Tooltip 文案需要面向新用户，解释透明 PNG 的透明像素 RGB 会影响追踪，给出如何选择背景色：白色图标通常用黑色背景；黑色/深色图标用白色背景更合适。
- “添加背景色”控件必须放在预处理参数列表最后（位于去噪之后），包含：
  - 不添加 / 添加 两个按钮
  - 颜色选择 input[type=color] + 颜色文本框（#RRGGBB）
  - 仅在启用“添加”时颜色输入可编辑；不启用时禁用
3) 默认值规则
- 图片没有透明通道：addBackground 默认 = null（不添加）
- 图片有透明通道：addBackground 默认 = #000000（黑色）
- 该默认值在 setRaster 导入新图片时自动判断并设置，但允许用户之后手动改为“不添加”或更换颜色
4) 预处理实现（关键逻辑）
- preprocessImageData(src, params) 输出 ImageData，并保证 alpha=255（拍扁）
- addBackground 逻辑：当 addBackground 为颜色值时，按 alpha 将原像素与该背景色合成；当为 null 时，不强制改变原行为
- 目标：避免透明 PNG 追踪出现“空白 SVG”或“黑底被当作形状导致扣洞/扣形状”

六、矢量化（右侧参数 + 自动生成）
1) Vectorize 参数（Zustand）
- nodeDensity (0-1)
- smoothing (0-1)
- speckleFilter (0-1)
- foregroundColor (#RRGGBB)
- backgroundColor (#RRGGBB)
- noBackground: boolean（语义：true=导出 SVG 时不包含背景 rect；false=导出时包含背景 rect；默认 true）
2) 自动生成策略（关键）
- 图片导入并完成预处理后，自动执行矢量化生成 SVG
- 当 vectorize 参数变化（nodeDensity/smoothing/speckleFilter/foregroundColor/backgroundColor/noBackground）时，自动重新生成 SVG（约 200ms debounce）
- 生成期间显示状态：生成 SVG…
3) 预览区（右侧）
- 显示生成 SVG；预览区容器背景使用 backgroundColor（即使 noBackground=true 也保持该预览背景，便于查看）
- SVG 容器必须使用与原图一致的宽高（width=原图宽，height=原图高），确保左右在同倍率下真实显示尺寸一致
4) 参数面板（VectorizePanel）
- 面板标题改为：SVG导出参数
- 不需要“配色”小标题
- 色值输入布局：主体色在左，背景色在右，中间有“交换颜色”按钮（ArrowLeftRight）
- 背景色的标题前放一个勾选框，勾选时表示“导出背景色”，不勾选时表示“不导出背景色”。勾选框后不需要额外说明小字。
5) 导出与交互
- 复制 SVG：复制 result.svgText 到剪贴板
- 下载 SVG：下载 .svg 文件，文件名基于原图名（去扩展名）
- 当有 SVG 可导出时，复制/下载按钮更明显的高亮样式；无 SVG 时 disabled
- 节点高亮按钮：toggle，启用时在 SVG 上叠加节点圆点用于检查路径节点

七、缩放/拖拽组件 ZoomPan（关键）
1) 功能
- 鼠标滚轮缩放（以鼠标所在点为缩放锚点）
- 拖拽平移（Pointer events）
- 顶部右侧浮层控件：复位按钮 + 倍率下拉（50/100/150/200/300%）
- 复位含义：倍率回到 100%，平移归零（fitScale 仍按容器与 contentSize 自动计算）
- 倍率与平移在左右两侧严格同步（共享 zoomScale 与 pan）
2) 交互与体验
- 浮层控件永远可点击（z-index 正确），并阻止事件冒泡，避免误触平移
- 拖动图形时不能选中任何文字：容器 select-none，拖拽开始时 preventDefault
- 在预览区域使用滚轮缩放时，不应触发网页整体上下滚动：需要用非被动监听（passive:false）的 wheel 事件拦截并 preventDefault
3) fitScale 策略
- fitScale = min(cw/w, ch/h)
- 最终显示 scale = fitScale * zoom（zoom 为相对适配倍率）

八、节点高亮（关键细节）
- 节点圆点叠加在 SVG 内（例如追加 [data-nodes='true'] 的 g/circle）
- 当预览缩放变化时，节点圆点显示大小保持屏幕像素恒定：r 需要按当前 activeScale 做反比补偿（或等效方式）
- 缩放时不应反复重建所有节点：优先只更新已有 circle 的 r，提高性能与稳定性

九、状态管理（Zustand）
- store 必须包含：raster、preprocessed、preprocessParams、vectorizeParams、result、isPreprocessing、isVectorizing、error
- setRaster 导入后立即触发预处理；runPreprocess / runVectorize 分离
- 预处理与矢量化执行需要 try/catch，并把错误写入 error

十、UI 细节（尽量复刻）
- 深色霓虹风格：按钮/面板/圆角/ring/透明度风格一致
- 纯信息标签（如图片尺寸、路径/节点统计）仅灰色文字，不要 ring 与胶囊底色
- “载入示例图”按钮默认灰色，hover 变亮
- 预览区工具条与参数面板间距紧凑

十一、目录结构要求
- src/pages/Home.tsx 为主页面
- src/components/PreprocessPanel.tsx, VectorizePanel.tsx, ZoomPan.tsx, SvgPreview.tsx, CanvasPreview.tsx 等组件拆分清晰
- src/utils/image/preprocess.ts 实现 preprocessImageData
- src/utils/vectorize/trace.ts 封装 imagetracerjs 调用并输出 svgText + pathCount + estimatedNodeCount
- src/utils/svg/ 包含 svg 清理与节点高亮实现
- 测试图片放在 src/assets/test-icons/

十二、验收标准（必须自测并确保满足）
1) 透明 PNG：白色图标 + 透明背景，默认 addBackground=黑色时能稳定生成可导出的 SVG（不再空白）
2) 透明污染：上传透明 PNG 但透明像素 RGB 为 0 等情况，合理选择 addBackground 后不会出现“外轮廓里扣洞/扣形状”的错误倾向
3) 导出背景：默认不导出背景（noBackground=true）；勾选“背景色”后导出的 SVG 会包含背景 rect；两种状态切换会自动重新生成 SVG
4) 同步视野：左右预览缩放倍率一致、实际显示尺寸一致；拖动一侧时另一侧视野同步
5) 滚轮体验：在预览区滚轮缩放不触发网页翻页滚动
6) 节点高亮：开启后节点点大小不随预览缩放变化；缩放/拖拽依旧流畅
7) tooltip：透明通道背景色说明 tooltip 不被底部遮挡、不会撑出页面底部空白
8) 页面无 badge：右下角不出现任何第三方 badge，项目无相关依赖与注入

请直接输出完整可运行的项目代码与关键文件内容，并确保 pnpm install && pnpm dev 能启动。"
