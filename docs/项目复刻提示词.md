claude \
  --dangerously-skip-permissions \
  --output-format=stream-json \
  --verbose \
  -p "请从零实现一个「图片转SVG矢量图标」纯前端 Web 应用（React 18 + TypeScript + Vite，pnpm，Tailwind CSS），要求在视觉布局、交互逻辑、参数行为与导出结果上精准复刻如下功能与运行效果。不要引入占位实现；所有功能必须可运行。

一、核心目标
- 用户导入一张图片（上传/拖拽/粘贴），左侧做预处理，右侧自动生成 SVG（矢量追踪）。
- 左右两个预览区必须支持缩放+拖动，并且缩放倍率与平移视野严格同步：用户在任意一侧拖动/缩放，另一侧看到的位置与倍率一致。
- 生成后的 SVG 支持复制/下载，支持“节点高亮”用于检查路径节点分布。

二、技术栈与依赖（必须使用这些，不要替换为其它库）
- react, react-dom, react-router-dom
- zustand（全局状态）
- tailwindcss + clsx + tailwind-merge（样式/合并）
- imagetracerjs（SVG 矢量追踪核心）
- lucide-react（图标）
- Vite 构建，TypeScript 工程化，提供 pnpm scripts: dev/build/preview/check（tsc -b --noEmit）

三、页面与布局（必须按此结构实现）
1) 页面标题区（顶部）
- 主标题：图片转SVG矢量图标
- 标题右侧同一行展示一个绿色 badge：图像 → 干净 SVG 路径
- 右上角放一个“使用说明”图标按钮（CircleHelp），鼠标悬停或点击均可显示说明弹层；点击后弹层可固定显示；点击弹层外区域关闭（hover 也可触发显示）。
- 说明弹层内容（中文）必须包含：
  - 使用步骤：1) 导入图片（拖入/粘贴/选择） 2) 左侧调预处理 3) 右侧自动生成SVG并调节点/平滑 4) 复制或下载
  - 工具特色：自动生成、可控输出（节点/平滑/斑点过滤）、便捷导出（复制/下载）、节点高亮

2) 主体双栏（桌面端为左右两列；小屏可上下堆叠但无需特别优化）
- 左列：预处理区域（上：导入工具条 + 预览；下：预处理参数面板）
- 右列：矢量化区域（上：导出/信息/节点高亮工具条 + 预览；下：矢量化参数面板）
- 左右预览区高度必须一致，且统一为 56vh（之前为 62vh，现要求缩小）。
- 预览区背景为深色网格/点阵质感（可复用现有实现风格），容器圆角 2xl、ring 细描边。

四、导入图片与空态提示
1) 导入方式
- 点击“选择文件”按钮打开文件选择（accept=image/*）
- 支持拖拽文件到左侧预览区域导入
- 支持粘贴剪贴板图片导入（window paste 监听，识别 image/* item）
- 提供“载入示例图”按钮生成内置示例（canvas 绘制后转 ImageData）
2) 空态引导
- 没有导入图片时：
  - “选择文件”按钮需要高亮（更明显的强调色）
  - 左侧预览区中央显示提示：拖入图片或者黏贴图片到此处
3) 拖拽反馈
- 拖拽悬停在预览区时显示明显 ring 高亮（例如 emerald ring-2）

五、预处理（左侧参数）
1) 预处理参数（Zustand 存储）
- mode: none | binary | grayscale
- invert: boolean
- threshold: number (0-255)
- contrast: number (-60 to 60)
- backgroundRemoval: number (0-1)
- edgeEnhance: number (0-1)
- denoise: number (0-1)
- addBackground: string | null  （新增：添加背景色）
2) UI（PreprocessPanel）
- 顶部：反相按钮 + Segmented（不处理/二值/灰度）
- Slider：阈值、对比度、背景去除、边缘增强、去噪
- “添加背景色”控件必须放在预处理参数列表最后（位于去噪之后），包含：
  - 不添加 / 添加 两个按钮
  - 颜色选择 input[type=color] + 颜色文本框（#RRGGBB）
  - 仅在启用“添加”时颜色输入可编辑；不启用时禁用
3) 默认值规则（关键）
- 图片没有透明通道：addBackground 默认 = null（不添加）
- 图片有透明通道：addBackground 默认 = #000000（黑色）
- 该默认值在 setRaster 导入新图片时自动判断并设置，但允许用户之后手动改为“不添加”或更换颜色
4) 预处理实现（关键逻辑）
- 预处理函数 preprocessImageData(src, params) 输出 ImageData，并保证 alpha=255（拍扁）
- addBackground 逻辑：当 addBackground 为颜色值时，按 alpha 将原像素与该背景色合成；当为 null 时，维持原先逻辑（若需要仍可按白色拍扁，但 addBackground=null 不应强制改变原行为）
- 目的：解决透明 PNG 图标追踪时出现“空白 SVG”或“黑底被当作形状扣洞”的问题

六、矢量化（右侧参数 + 自动生成）
1) Vectorize 参数（Zustand 存储）
- nodeDensity (0-1)
- smoothing (0-1)
- speckleFilter (0-1)
- backgroundColor (#RRGGBB)
- foregroundColor (#RRGGBB)
2) 自动生成策略（关键）
- 图片导入并完成预处理后，自动执行矢量化生成 SVG
- 当 vectorize 参数变化（nodeDensity/smoothing/speckleFilter/backgroundColor/foregroundColor）时，自动重新生成 SVG（建议 200ms 左右 debounce）
- 生成期间显示状态：生成 SVG…
3) 预览区（右侧）
- 显示生成 SVG；预览背景色使用 vectorize.backgroundColor
- SVG 容器必须使用与原图一致的宽高（width=原图宽，height=原图高），确保左右在同倍率下真实显示尺寸一致
4) 导出与交互
- 复制 SVG：复制 result.svgText 到剪贴板
- 下载 SVG：下载 .svg 文件，文件名基于原图名（去扩展名）
- 当有 SVG 可导出时，复制/下载按钮需要更明显的高亮样式；无 SVG 时为普通样式且 disabled
- 节点高亮按钮：toggle；启用时在 SVG 上叠加节点高亮（使用已有 svg 节点分析/覆盖方式实现），用于查看路径节点
- “节点高亮”按钮前有合适图标（例如 Scan），且与按钮文字对齐

七、缩放/拖拽组件 ZoomPan（关键）
1) 功能
- 鼠标滚轮缩放（以鼠标所在点为缩放锚点）
- 拖拽平移（Pointer events）
- 顶部右侧浮层控件：复位按钮 + 倍率下拉（50/100/150/200/300%）
- 复位含义：适应窗口大小（fit/contain）并居中，同时倍率回到 100%，平移归零
- 倍率下拉与左右预览同步（共享 zoomScale 状态）
- 平移 tx/ty 与左右预览同步（共享 pan 状态）
2) 交互细节
- 浮层控件必须永远可点击，不被内容遮挡（正确 z-index）
- 拖动图形时不能选中任何文字（包括“复位/100%”），需要从根本修复：容器 select-none，并在拖拽开始时 preventDefault
- 浮层控件区域需要阻止事件冒泡，避免拖动时误触发平移
3) 适应窗口（fitScale）策略（关键）
- ZoomPan 内部维护 fitScale：根据容器大小与 contentSize(width/height) 计算 min(cw/w, ch/h)
- 最终显示 scale = fitScale * zoom（其中 zoom 由下拉/滚轮控制，表示相对适配倍率）
- 左右两侧使用同一个 contentSize（来自原图尺寸），确保同倍率下左右真实像素尺寸一致

八、状态管理（Zustand）
- store 中必须包含：raster、preprocessed、preprocessParams、vectorizeParams、result、isPreprocessing、isVectorizing、error
- setRaster 导入后立即触发预处理；runPreprocess / runVectorize 分离
- 默认参数值应与现有实现一致（可合理设定，但需包含上文所有字段）
- 预处理与矢量化的执行需要 try/catch 并把错误写入 error

九、UI 细节（尽量复刻现有风格）
- 按钮、面板、ring、圆角、背景透明度等采用深色霓虹风格；不要出现多余的“可点击”误导边框：纯信息标签（如图片尺寸、路径/节点统计）仅显示为灰色文字，不要有 ring 与背景胶囊底色
- “载入示例图”按钮文字默认灰色，hover 变亮
- 预览区工具条与参数面板间距紧凑

十、目录结构要求
- src/pages/Home.tsx 为主页面
- src/components/PreprocessPanel.tsx, VectorizePanel.tsx, ZoomPan.tsx, SvgPreview.tsx, CanvasPreview.tsx 等组件拆分清晰
- src/utils/image/preprocess.ts 内实现 preprocessImageData
- src/utils/vectorize/trace.ts 内封装 imagetracerjs 调用并输出 svgText + pathCount + estimatedNodeCount
- src/utils/svg/ 内包含 svg 清理、节点高亮相关实现
- 将测试图片放在 src/assets/test-icons/（用于仓库自带测试素材）

十一、验收标准（你实现后必须自测并确保满足）
- 上传带透明通道的白色图标 PNG：默认 addBackground 为黑色时能生成可导出的 SVG（不再空白）
- 上传带透明通道且带黑底污染的 PNG：启用 addBackground 合成后不会出现“外轮廓里扣洞扣形状”的错误倾向，主体更稳定
- 左右预览缩放倍率一致，实际显示尺寸一致；拖动一侧时另一侧视野同步
- 拖动预览时不会选中文本，“复位/倍率”不再被框选
- 复制/下载仅在有 SVG 时可用且高亮，节点高亮可切换

请直接输出完整可运行的项目代码与关键文件内容，并确保 pnpm install && pnpm dev 能启动。"